# Tasks for PR #81: godot plugin skill

**Repository:** jkatigb/agentctl
**URL:** https://github.com/jkatigb/agentctl/pull/81

## 📝 Review Comments to Address (9)

### Task 1: coderabbitai[bot]'s feedback

## Walkthrough
Introduces a complete Godot Editor integration skill for agentctl with a plugin bridge (bridge.gd) exposing editor APIs via HTTP/JSON, CLI commands (godot, godot plugin-install) for scene and node operations, a context/filter skill for LLM-driven content selection, comprehensive test coverage, and design specifications with workflow documentation.
## Changes
| Cohort / File(s) | Summary |
|---|---|
| **Godot Editor Integration Skill** <br> `skills/editor_godot/main.go`, `skills/editor_godot/main_test.go`, `skills/editor_godot/skill.yaml` | New Go-based skill orchestrating Godot editor operations through the bridge plugin; handles action routing, parameter building, HTTP communication, error handling, and optional CAS storage for large responses. |
| **Godot EditorPlugin** <br> `skills/editor_godot/godot_plugin/bridge.gd`, `skills/editor_godot/godot_plugin/plugin.cfg`, `skills/editor_godot/godot_plugin/README.md` | New GDScript EditorPlugin implementing a TCP/HTTP server (port 7777) that routes requests to scene/node manipulation handlers (inspect, create, delete, rename, reparent, set property, attach script, connect signal, class info, scene save, resource list, run/stop game, errors) with undo/redo support and workspace validation. |
| **CLI Godot Commands** <br> `cmd/agentctl/cmd/godot.go`, `cmd/agentctl/cmd/godot_plugin.go`, `cmd/agentctl/cmd/godot_test.go` | New CLI command group with godot subcommands (install, ping, scene-tree, inspect, create, delete, rename, reparent, set, attach-script, connect-signal, class-info, save, resources, run, stop, errors) and plugin-install installer; includes comprehensive test coverage for command structure, flags, and argument validation. |
| **Context Filter Skill** <br> `skills/context_filter/main.go`, `skills/context_filter/skill.yaml` | New skill implementing LLM-backed context/filtering with support for multiple LLM providers, CAS integration, budget-aware chunk selection, and structured JSON request/response handling. |
| **Documentation** <br> `docs/godot/editor_integration.md`, `docs/spec/context_filter.md`, `docs/workflows/exa_mcp_context_filter.md` | New design specifications and workflow documentation covering Godot integration architecture, context filter behavior, error codes, security considerations, and end-to-end examples. |
| **Build/Test Updates** <br> `cmd/agentctl/cmd/run_test.go` | Removed forced CGO_ENABLED=1 override; allows CGO settings to inherit from parent process or CI environment. |
| **Cleanup** <br> `pr77_comments.md` | Deleted PR review notes file. |
## Sequence Diagram(s)
```mermaid
sequenceDiagram
    participant CLI as agentctl CLI
    participant Skill as editor/godot Skill
    participant Bridge as GodotAIBridge Plugin
    participant Godot as Godot Editor
    CLI->>Skill: action + params (host, port, node_path, ...)
    activate Skill
    Skill->>Skill: parseInput, validateInput, buildParams
    Skill->>Bridge: HTTP POST: {action, params, workspace_root}
    activate Bridge
    Bridge->>Bridge: _handle_request (route to handler)
    Bridge->>Bridge: _validate_workspace (check mismatch)
    Bridge->>Godot: editor/node manipulation (via Godot API)
    Godot->>Bridge: result
    Bridge->>Bridge: undo/redo if applicable
    Bridge->>Skill: JSON response {status, data, error?}
    deactivate Bridge
    Skill->>Skill: emitSuccess or fail (CAS storage if large)
    Skill->>CLI: envelope {data, summary, artifact?}
    deactivate Skill
```
```mermaid
sequenceDiagram
    participant User as User/LLM
    participant Skill as context/filter Skill
    participant LLM as LLM Provider
    participant CAS as Content Storage
    User->>Skill: {prompt, scope, source, budget, llm}
    activate Skill
    Skill->>Skill: normalize defaults, validate fields
    Skill->>Skill: build candidate chunks from source/CAS
    Skill->>Skill: craft LLM prompt with constraints
    Skill->>LLM: POST: {prompt, candidates, budget}
    activate LLM
    LLM->>LLM: select & score chunks
    LLM->>Skill: {chunks, summary, approx_tokens}
    deactivate LLM
    Skill->>Skill: align & filter by budget
    alt oversized output
        Skill->>CAS: store full result
        Skill->>User: {summary, cas_digest, hint}
    else normal output
        Skill->>User: {chunks, summary, approx_tokens}
    end
    deactivate Skill
```
## Estimated code review effort
🎯 4 (Complex) | ⏱️ ~50–70 minutes
**Key areas requiring careful review:**
- **skills/editor_godot/godot_plugin/bridge.gd** (~1000+ lines) — Extensive GDScript plugin with 25+ handler methods, request routing, undo/redo integration, workspace validation, path resolution, and type conversion logic; requires verification of scene manipulation correctness and error edge cases.
- **skills/editor_godot/main.go** (~400+ lines) — HTTP client communication, action routing, parameter building, error handling, and CAS storage logic; needs validation of timeout handling, envelope emission, and bridge error classification.
- **skills/context_filter/main.go** (~500+ lines) — LLM provider abstraction, prompt construction, budget semantics, CAS integration, and chunk selection logic; requires scrutiny of token counting, budget enforcement, and error propagation.
- **CLI command wiring** (godot.go, godot_plugin.go) — Ensure proper flag initialization, payload construction, and error envelope formatting align with plugin expectations.
- **Test coverage** — Validate that test assertions properly cover action-specific validation and error scenarios.
## Suggested labels
`ai-generated`
## Poem
> 🐰 *A bridge of code, from Go to GDScript so fine,*
> *Scene trees dance and signals align,*
> *Filter context, chunk by gleaming chunk,*
> *With undo/redo, no feature skipped or flunk!*
> *CLI meets Plugin, a harmonious dance,*
> *Godot's editor yields to our command's advance.* 🎮✨
## Pre-merge checks and finishing touches
<details>
<summary>❌ Failed checks (2 warnings, 1 inconclusive)</summary>
|     Check name     | Status         | Explanation                                                                                                                                                                | Resolution                                                                                                                                |
| :----------------: | :------------- | :------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | :---------------------------------------------------------------------------------------------------------------------------------------- |
|  Description check | ⚠️ Warning     | The description is incomplete, with multiple commit message lines but missing the summary section content and testing checklist items are unchecked rather than completed. | Fill in the 'Summary' section with a concise overview of changes, and update testing checkboxes to indicate completion status.            |
| Docstring Coverage | ⚠️ Warning     | Docstring coverage is 0.00% which is insufficient. The required threshold is 80.00%.                                                                                       | You can run `@coderabbitai generate docstrings` to improve docstring coverage.                                                            |
|     Title check    | ❓ Inconclusive | The title 'godot plugin skill' is vague and generic, using non-descriptive terms that don't clearly convey the main purpose of the changeset.                              | Use a more specific title that highlights the primary change, such as 'Add Godot Editor integration skill with plugin bridge' or similar. |
</details>
<details>
<summary>✨ Finishing touches</summary>
- [ ]  📝 Generate docstrings
<details>
<summary>🧪 Generate unit tests (beta)</summary>
- [ ]    Create PR with unit tests
- [ ]    Post copyable unit tests in a comment
- [ ]    Commit unit tests in branch `feature/godot-plugin`
</details>
</details>
> [!TIP]
> <details>
> <summary>📝 Customizable high-level summaries are now available in beta!</summary>
>
> You can now customize how CodeRabbit generates the **high-level summary** in your pull requests — including its content, structure, tone, and formatting.
>
> - Provide your own instructions using the `high_level_summary_instructions` setting.
> - Format the summary however you like (bullet lists, tables, multi-section layouts, contributor stats, etc.).
> - Use `high_level_summary_in_walkthrough` to move the summary from the description to the walkthrough section.
>
> **Example instruction:**
>
> > "Divide the high-level summary into five sections:
> >  1. **📝 Description** — Summarize the main change in 50–60 words, explaining what was done.
> >  2. **📓 References** — List relevant issues, discussions, documentation, or related PRs.
> >  3. **📦 Dependencies & Requirements** — Mention any new/updated dependencies, environment variable changes, or configuration updates.
> >  4. **📊 Contributor Summary** — Include a Markdown table showing contributions:
> >     `| Contributor | Lines Added | Lines Removed | Files Changed |`
> >  5. **✔️ Additional Notes** — Add any extra reviewer context.
> >  Keep each section concise (under 200 words) and use bullet or numbered lists for clarity."
>
> Note: This feature is currently in beta for Pro-tier users, and pricing will be announced later.
>
> </details>
---
<sub>Comment `@coderabbitai help` to get the list of available commands and usage tips.</sub>

---

### Task 2: coderabbitai[bot]'s feedback

**File:** `cmd/agentctl/cmd/godot_plugin/bridge.gd:13`

<details>
<summary>🧩 Analysis chain</summary>
Script executed:
```shell
#!/bin/bash
# First, verify both files exist and check their sizes
echo "=== Checking file existence and sizes ==="
ls -lh cmd/agentctl/cmd/godot_plugin/bridge.gd skills/editor_godot/godot_plugin/bridge.gd 2>&1
echo -e "\n=== Comparing file line counts ==="
wc -l cmd/agentctl/cmd/godot_plugin/bridge.gd skills/editor_godot/godot_plugin/bridge.gd 2>&1
echo -e "\n=== Running diff comparison ==="
diff -q cmd/agentctl/cmd/godot_plugin/bridge.gd skills/editor_godot/godot_plugin/bridge.gd && echo "✓ Files are identical" || echo "✗ Files differ"
echo -e "\n=== Detailed diff output (first 50 lines of differences if any) ==="
diff cmd/agentctl/cmd/godot_plugin/bridge.gd skills/editor_godot/godot_plugin/bridge.gd | head -50
```
Repository: jkatigb/agentctl
---
<shell_result>
Repository: jkatigb/agentctl
Exit code: 0
stdout:
```
=== Checking file existence and sizes ===
-rw-r--r-- 1 jailuser jailuser 29K Nov 26 17:44 cmd/agentctl/cmd/godot_plugin/bridge.gd
-rw-r--r-- 1 jailuser jailuser 29K Nov 26 17:44 skills/editor_godot/godot_plugin/bridge.gd
=== Comparing file line counts ===
  947 cmd/agentctl/cmd/godot_plugin/bridge.gd
  947 skills/editor_godot/godot_plugin/bridge.gd
 1894 total
=== Running diff comparison ===
✓ Files are identical
=== Detailed diff output (first 50 lines of differences if any) ===
```
</shell_result>
---
</details>
**Duplicate file confirmed: identical to `skills/editor_godot/godot_plugin/bridge.gd`.**
This entire file is a verified duplicate of `skills/editor_godot/godot_plugin/bridge.gd` (947 lines, 29K). Having two identical copies creates maintenance burden and divergence risk.
Consider one of:
1. **Remove this copy** and use the skills version as the source of truth
2. **Generate this copy at build time** from the skills version (e.g., via `go:embed` or a Makefile target)
3. **Symlink** if the filesystem supports it
4. **Document** which copy is authoritative if both must exist

---

### Task 3: coderabbitai[bot]'s feedback

**File:** `docs/workflows/exa_mcp_context_filter.md:133`

**Make the `exa_text.txt` → `context/filter` example JSON-safe**
In the §4 example, this line:
```bash
"source": {
  "text": "'"$(tr '\n' ' ' < exa_text.txt)"'"
}
```
injects the file contents directly into a JSON string. If `exa_text.txt` contains quotes or backslashes, the resulting JSON will be invalid.
You already use a safer pattern later with `jq -Rs`. Consider rewriting this example to mirror that style, for example:
```bash
jq -Rs '{
    prompt: "Explain how agentctl handles CAS integrity failures.",
    scope: "code",
    source: { text: . },
    budget: { target_tokens: 2000, max_chunks: 16 },
    llm: { provider: "groq", model: "llama-3.3-70b-versatile" }
  }' exa_text.txt \
  | ./bin/agentctl run context/filter --input-file -
```
This keeps the docs robust for arbitrary Exa output.
<details>
<summary>🧰 Tools</summary>
<details>
<summary>🪛 LanguageTool</summary>
[style] ~105-~105: As an alternative to the over-used intensifier ‘very’, consider replacing this phrase.
Context: ...er`.  1. Flatten Exa content into text (very simple example):     ```bash    jq -r '.data.c...
(EN_WEAK_ADJECTIVE)
</details>
</details>
<details>
<summary>🤖 Prompt for AI Agents</summary>
```
In docs/workflows/exa_mcp_context_filter.md around lines 105-133, the example
injects exa_text.txt contents directly into a JSON string which breaks when the
file contains quotes/backslashes; replace that unsafe inline substitution with
the jq -Rs pattern that reads the file as raw text, constructs the JSON object
(prompt, scope, source.text, budget, llm) safely, and pipes the resulting JSON
to agentctl using the CLI's input-file/- option so arbitrary Exa output is
JSON-safe.
```
</details>

---

### Task 4: coderabbitai[bot]'s feedback

**File:** `skills/context_filter/main.go:164`

<details>
<summary>🧩 Analysis chain</summary>
OpenAI gpt-4.1-mini current model 2025
---
<web_search_result>
Yes — OpenAI’s GPT‑4.1‑mini is an official model (launched April 14, 2025). Key facts:
- Launched as part of the GPT‑4.1 family (models: gpt‑4.1, gpt‑4.1‑mini, gpt‑4.1‑nano). [1]
- Context window: ~1,047,576 tokens (1M). [2]
- Suited for instruction following, tool-calling, low-latency uses; OpenAI positions it as a smaller, faster alternative to GPT‑4.1. [1][2]
- Pricing (per 1M tokens, example): input $0.40 / cached input $0.10 / output $1.60. [1][2]
- Available in the OpenAI API (chat, responses, realtime, batch) and rolled into ChatGPT for paid users per OpenAI release notes. [1][2][3]
Sources: OpenAI announcement (Introducing GPT‑4.1) and OpenAI model docs. [1][2]
</web_search_result>
---
Anthropic claude-3-5-sonnet-latest current model
---
<web_search_result>
- The alias claude-3-5-sonnet-latest points to the snapshot model ID claude-3-5-sonnet-20241022 (upgrade of the original 20240620 snapshot). [1][2]
- Anthropic announced deprecation of the Claude Sonnet 3.5 models on August 13, 2025 and scheduled retirement in October 2025 (docs list retirement dates of Oct 22, 2025 and Oct 28, 2025 in different release-note pages). They recommend migrating to Claude Sonnet 4 (e.g., claude-sonnet-4-20250514 / sonnet 4.5 aliases). [3][4]
Sources:
[1] Anthropic models overview (models list showing claude-3-5-sonnet-latest → claude-3-5-sonnet-20241022).
[2] Anthropic models table (model snapshots and IDs).
[3] Anthropic release notes — deprecation announcement (Aug 13, 2025).
[4] Anthropic release notes / developer docs (deprecation + retirement scheduling).
</web_search_result>
---
Google Gemini gemini-1.5-pro-latest current model
---
<web_search_result>
Short answer: gemini-1.5-pro-latest was the auto-updating alias that pointed to the Gemini 1.5 Pro stable build (most recently gemini-1.5-pro-002), but the Gemini 1.5 family was deprecated/retired in September 2025 and should no longer be used for new projects. [1][2]
Sources:
- Gemini models docs (shows alias, token limits and that latest = gemini-1.5-pro-latest / stable = gemini-1.5-pro-002). [1]
- Gemini changelog / Vertex AI docs (deprecation/retirement notices in Sept–Oct 2025 and migration guidance). [2]
</web_search_result>
---
Groq llama-3.1-70b-versatile current model
---
<web_search_result>
Short answer: groq's llama-3.1-70b-versatile has been decommissioned/ deprecated — Groq replaced it with llama-3.3-70b-versatile (requests to the 3.1 70B model were shut down on Jan 24, 2025). See Groq's deprecations page and blog for details. [1][2]
Sources
- Groq model deprecations (decommission schedule, recommended replacement). [1]
- Groq blog/announcements about Llama 3.3 70B availability and performance on GroqCloud. [2]
</web_search_result>
---
OpenRouter openrouter/auto model identifier
---
<web_search_result>
The OpenRouter auto router model identifier is: openrouter/auto. [1][2]
Sources:
[1] OpenRouter docs — Model routing / Auto Router.
[2] OpenRouter (openrouter.ai) — Auto Model Selection.
</web_search_result>
---
</details>
**Three of five default LLM models are deprecated or decommissioned and must be updated.**
The default models have the following issues:
- **`gpt-4.1-mini`** (OpenAI): Valid — launched April 14, 2025.
- **`claude-3-5-sonnet-latest`** (Anthropic): **Deprecated** — Anthropic deprecated Claude 3.5 Sonnet on August 13, 2025 with retirement scheduled for October 22–28, 2025. Migrate to `claude-sonnet-4-20250514` (Claude Sonnet 4.5).
- **`gemini-1.5-pro-latest`** (Google): **Deprecated** — The Gemini 1.5 family was deprecated and retired in September–October 2025. Use `gemini-2.0-flash-latest` or an appropriate Gemini 2.0 model instead.
- **`llama-3.1-70b-versatile`** (Groq): **Decommissioned** — Groq shut down the Llama 3.1 70B model on January 24, 2025. Replace with `llama-3.3-70b-versatile`.
- **`openrouter/auto`** (OpenRouter): Valid — confirmed as a legitimate auto-routing identifier.
Update lines 149–164 to use current model identifiers or the fallback logic will fail at runtime.

---

### Task 5: coderabbitai[bot]'s feedback

**File:** `skills/context_filter/main.go:510`

**Add HTTP client timeout to prevent indefinite hangs.**
The code uses `http.DefaultClient` which has no default timeout. If a provider becomes unresponsive, the skill could hang indefinitely. Consider using a client with a configured timeout.
```diff
+var httpClient = &http.Client{
+	Timeout: 60 * time.Second,
+}
+
 func callOpenAICompatible(ctx context.Context, llm llmInput, prompt string) (string, map[string]any, error) {
 	// ... existing code ...
-	resp, err := http.DefaultClient.Do(req)
+	resp, err := httpClient.Do(req)
```
Apply the same change to `callAnthropic` (line 587) and `callGemini` (line 667).
> Committable suggestion skipped: line range outside the PR's diff.
<details>
<summary>🤖 Prompt for AI Agents</summary>
```
In skills/context_filter/main.go around lines 502 to 510 (and similarly update
callAnthropic at ~587 and callGemini at ~667), the code uses http.DefaultClient
(no timeout) which can hang indefinitely; replace usages with a local
http.Client that has a reasonable Timeout set (e.g., configurable or a fixed
value like 30s), and use that client to perform Do(req). Ensure you import time
if needed and keep the same defer close logic; apply the identical change to the
callAnthropic and callGemini functions so all external HTTP calls use a
timeout-bound client.
```
</details>

---

### Task 6: coderabbitai[bot]'s feedback

**File:** `skills/editor_godot/godot_plugin/bridge.gd:23`

**Error buffer is declared but never populated.**
The `_error_buffer` array is initialized and read in `_handle_errors`, but no code ever appends errors to it. The comment at lines 36-38 acknowledges this limitation but doesn't implement any solution.
Consider either:
1. Remove the error buffer feature if it's not implemented
2. Add a mechanism to capture errors (e.g., override `_print_error` or use a custom logger)
```diff
 var _error_buffer: Array[Dictionary] = []
 const MAX_ERROR_BUFFER: int = 200
+
+
+func _add_error(message: String, source: String = "") -> void:
+	_error_buffer.append({
+		"message": message,
+		"source": source,
+		"timestamp": Time.get_unix_time_from_system(),
+	})
+	if _error_buffer.size() > MAX_ERROR_BUFFER:
+		_error_buffer.pop_front()
```
<details>
<summary>📝 Committable suggestion</summary>
> ‼️ **IMPORTANT**
> Carefully review the code before committing. Ensure that it accurately replaces the highlighted code, contains no missing lines, and has no issues with indentation. Thoroughly test & benchmark the code to ensure it meets the requirements.
```suggestion
var _error_buffer: Array[Dictionary] = []
const MAX_ERROR_BUFFER: int = 200
func _add_error(message: String, source: String = "") -> void:
	_error_buffer.append({
		"message": message,
		"source": source,
		"timestamp": Time.get_unix_time_from_system(),
	})
	if _error_buffer.size() > MAX_ERROR_BUFFER:
		_error_buffer.pop_front()
```
</details>
<details>
<summary>🤖 Prompt for AI Agents</summary>
```
In skills/editor_godot/godot_plugin/bridge.gd around lines 22-23, the
_error_buffer is allocated but never written to; update the file to either
remove the unused buffer and any code that reads it, or implement error capture
by overriding _print_error (or hooking the engine's error signal) to append a
Dictionary (message, stack, timestamp, severity) to _error_buffer and enforce
the MAX_ERROR_BUFFER cap by trimming older entries when pushing new ones so
_handle_errors actually has populated data to process.
```
</details>

---

### Task 7: coderabbitai[bot]'s feedback

**File:** `skills/editor_godot/godot_plugin/bridge.gd:529`

**Signal connections are runtime-only and won't persist when scene is saved.**
Connections made via `source.connect()` exist only at runtime. When the scene is saved and reloaded, these connections will be lost. For persistent signal connections, you need to modify the scene's connection data.
Consider adding a warning in the response or documenting this limitation clearly:
```diff
 return _success_response({
 	"ok": true,
 	"source_path": source_path,
 	"signal_name": signal_name,
 	"target_path": target_path,
 	"method_name": method_name,
+	"warning": "Connection is runtime-only and will not persist after scene reload. For persistent connections, edit the scene file directly.",
 })
```
<details>
<summary>📝 Committable suggestion</summary>
> ‼️ **IMPORTANT**
> Carefully review the code before committing. Ensure that it accurately replaces the highlighted code, contains no missing lines, and has no issues with indentation. Thoroughly test & benchmark the code to ensure it meets the requirements.
```suggestion
	# Connect signal (note: undo/redo for signals is complex, doing simple connect for now)
	if source.is_connected(signal_name, Callable(target, method_name)):
		return _success_response({
			"ok": true,
			"already_connected": true,
		})
	var err := source.connect(signal_name, Callable(target, method_name))
	if err != OK:
		return _error_response("ESIGNAL_CONNECT", "Failed to connect signal: error %d" % err, "")
	return _success_response({
		"ok": true,
		"source_path": source_path,
		"signal_name": signal_name,
		"target_path": target_path,
		"method_name": method_name,
		"warning": "Connection is runtime-only and will not persist after scene reload. For persistent connections, edit the scene file directly.",
	})
```
</details>
<details>
<summary>🤖 Prompt for AI Agents</summary>
```
In skills/editor_godot/godot_plugin/bridge.gd around lines 512 to 529, the code
performs a runtime-only source.connect() which will not persist when the scene
is saved; change the response to explicitly warn about this limitation and/or
persist the connection in the scene data: update the success response to include
a warning field (e.g., "runtime_only": true or "warning": "Signal connection is
runtime-only and won’t persist when scene is saved") and/or implement
persistence by editing the source scene’s connection data (add the connection
entry to the scene/resource so it is serialized) before returning success so
callers know the connection won’t survive a reload unless stored.
```
</details>

---

### Task 8: coderabbitai[bot]'s feedback

**File:** `skills/editor_godot/godot_plugin/bridge.gd:561`

**Missing `add_do_reference` may cause undo to fail after node deletion.**
When deleting a node, you call `add_undo_reference` but not `add_do_reference`. After the do action removes the child, the node may be freed. When undo tries to re-add it, the reference could be invalid.
```diff
 _undo_redo.create_action("AI: Delete Node " + node_name)
+_undo_redo.add_do_reference(node)
 _undo_redo.add_do_method(parent, "remove_child", node)
 _undo_redo.add_undo_method(parent, "add_child", node)
 _undo_redo.add_undo_method(parent, "move_child", node, node_index)
 _undo_redo.add_undo_method(node, "set_owner", root)
 _undo_redo.add_undo_reference(node)
 _undo_redo.commit_action()
```
<details>
<summary>📝 Committable suggestion</summary>
> ‼️ **IMPORTANT**
> Carefully review the code before committing. Ensure that it accurately replaces the highlighted code, contains no missing lines, and has no issues with indentation. Thoroughly test & benchmark the code to ensure it meets the requirements.
```suggestion
	_undo_redo.create_action("AI: Delete Node " + node_name)
	_undo_redo.add_do_reference(node)
	_undo_redo.add_do_method(parent, "remove_child", node)
	_undo_redo.add_undo_method(parent, "add_child", node)
	_undo_redo.add_undo_method(parent, "move_child", node, node_index)
	_undo_redo.add_undo_method(node, "set_owner", root)
	_undo_redo.add_undo_reference(node)
	_undo_redo.commit_action()
```
</details>
<details>
<summary>🤖 Prompt for AI Agents</summary>
```
In skills/editor_godot/godot_plugin/bridge.gd around lines 555 to 561, the undo
action adds an undo reference but never a do reference, so the node can be freed
after the remove_child do and the undo will hold a dangling reference; fix by
calling _undo_redo.add_do_reference(node) before adding the do method that
removes the child (i.e., add the do-reference prior to
_undo_redo.add_do_method(parent, "remove_child", node)), keep the existing
add_undo_reference call, and then commit the action so the engine retains the
node for the do step and still restores it on undo.
```
</details>

---

### Task 9: coderabbitai[bot]'s feedback

**File:** `skills/editor_godot/godot_plugin/bridge.gd:853`

**Type conversion falls through silently on validation failure.**
When `value_str.is_valid_int()` returns false for `TYPE_INT`, the code falls through to the `str_to_var` fallback and eventually returns the string. This could cause type mismatches where a property expects an int but receives a string.
Consider returning `null` explicitly when type-specific validation fails:
```diff
 match target_type:
 	TYPE_INT:
 		if value_str.is_valid_int():
 			return value_str.to_int()
+		return null
 	TYPE_FLOAT:
 		if value_str.is_valid_float():
 			return value_str.to_float()
+		return null
 	TYPE_BOOL:
 		var lower := value_str.to_lower()
 		if lower == "true":
 			return true
 		if lower == "false":
 			return false
+		return null
```
<details>
<summary>📝 Committable suggestion</summary>
> ‼️ **IMPORTANT**
> Carefully review the code before committing. Ensure that it accurately replaces the highlighted code, contains no missing lines, and has no issues with indentation. Thoroughly test & benchmark the code to ensure it meets the requirements.
```suggestion
	match target_type:
		TYPE_INT:
			if value_str.is_valid_int():
				return value_str.to_int()
			return null
		TYPE_FLOAT:
			if value_str.is_valid_float():
				return value_str.to_float()
			return null
		TYPE_BOOL:
			var lower := value_str.to_lower()
			if lower == "true":
				return true
			if lower == "false":
				return false
			return null
		TYPE_STRING:
			return value_str
		TYPE_VECTOR2:
			return _parse_vector2(value_str)
		TYPE_VECTOR3:
			return _parse_vector3(value_str)
		TYPE_COLOR:
			return _parse_color(value_str)
```
</details>
<details>
<summary>🤖 Prompt for AI Agents</summary>
```
In skills/editor_godot/godot_plugin/bridge.gd around lines 832 to 853, the match
arms for TYPE_INT, TYPE_FLOAT, and TYPE_BOOL silently fall through when
validation fails, causing str_to_var fallback to return a string; change each
type-specific branch to return null explicitly when validation fails (e.g., if
is_valid_int/is_valid_float checks fail, return null; for BOOL convert/lower
check, return null if not "true" or "false"); ensure
TYPE_VECTOR2/TYPE_VECTOR3/TYPE_COLOR parsing routines also propagate null on
parse failure rather than falling through.
```
</details>

---

## Summary

**Total tasks:** 9
- 📝 Review comments: 9
