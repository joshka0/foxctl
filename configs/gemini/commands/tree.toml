# Directory tree command
description = "Show project structure"

prompt = """Show the directory structure:

```bash
!{agentctl run fs/tree --input '{"path": "{{args}}", "max_depth": 3}'}
```

Analyze the structure:
- Identify main components/modules
- Note the organizational pattern
- Highlight key files"""
