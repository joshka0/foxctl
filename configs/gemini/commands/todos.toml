# Task list command
description = "Show current tasks from foxctl"

prompt = """Show current tasks:

```bash
!{foxctl todo list}
```

Summarize:
- Active tasks and their status
- Blocked or pending items
- Suggested next actions"""
