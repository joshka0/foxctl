{
  "name": "foxctl-opencode-hooks",
  "version": "1.0.0",
  "type": "module",
  "description": "OpenCode plugins for foxctl integration",
  "main": "index.ts",
  "scripts": {
    "test": "bun test",
    "typecheck": "tsc --noEmit",
    "build": "bun build ./index.ts --outdir dist"
  },
  "dependencies": {
    "@opencode-ai/plugin": "latest",
    "zod": "^4.0.0"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "typescript": "^5.7.0"
  }
}
