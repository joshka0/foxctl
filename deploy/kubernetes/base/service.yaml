apiVersion: v1
kind: Service
metadata:
  name: agentctl
  namespace: agentctl
  labels:
    app.kubernetes.io/name: agentctl
    app.kubernetes.io/component: core
spec:
  type: ClusterIP
  ports:
  - name: http
    port: 8080
    targetPort: health
    protocol: TCP
  selector:
    app.kubernetes.io/name: agentctl
    app.kubernetes.io/component: core
