apiVersion: v1
kind: Service
metadata:
  name: gui-auth-gateway
  namespace: agentctl
  labels:
    app.kubernetes.io/name: agentctl
    app.kubernetes.io/component: gui-auth-gateway
spec:
  type: ClusterIP
  ports:
  - name: http
    port: 8080
    targetPort: http
    protocol: TCP
  selector:
    app.kubernetes.io/name: agentctl
    app.kubernetes.io/component: gui-auth-gateway
