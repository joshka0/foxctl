apiVersion: v1
kind: Service
metadata:
  name: gui-auth-gateway
  labels:
    app.kubernetes.io/name: foxctl
    app.kubernetes.io/component: gui-auth-gateway
spec:
  type: ClusterIP
  ports:
  - name: http
    port: 8080
    targetPort: http
    protocol: TCP
  selector:
    app.kubernetes.io/name: foxctl
    app.kubernetes.io/component: gui-auth-gateway
