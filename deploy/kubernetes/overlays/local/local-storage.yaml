apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: agentctl-local-cache
  namespace: agentctl
  labels:
    app.kubernetes.io/name: agentctl
    app.kubernetes.io/component: storage
spec:
  accessModes:
  - ReadWriteOnce
  resources:
    requests:
      storage: 5Gi
