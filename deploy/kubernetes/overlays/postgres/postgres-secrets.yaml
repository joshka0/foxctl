# PostgreSQL connection secrets
# IMPORTANT: Replace placeholder values before applying.
# Consider using ExternalSecrets or Sealed Secrets in production.

apiVersion: v1
kind: Secret
metadata:
  name: postgres-secrets
  namespace: foxctl
  labels:
    app.kubernetes.io/name: foxctl
    app.kubernetes.io/component: database
type: Opaque
stringData:
  # PostgreSQL DSN (required)
  # For RDS: postgres://user:pass@my-rds-instance.region.rds.amazonaws.com:5432/foxctl?sslmode=require
  # For local: postgres://postgres:dev@postgres:5432/foxctl?sslmode=disable
  dsn: "postgres://foxctl:CHANGE_ME@your-rds-instance.region.rds.amazonaws.com:5432/foxctl?sslmode=require"
