[install]
# Use exact versions for reproducibility
exact = true
# Keep dependency lifecycle scripts disabled unless a package is explicitly
# trusted after supply-chain review.
ignoreScripts = true

[run]
# Enable hot reloading for development
bun = true
