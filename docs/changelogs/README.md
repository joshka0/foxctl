# Changelog Notes

Historical change notes and implementation summaries.

## Conventions

- File naming follows `YYYY-MM-DD_<slug>.md`.
- These docs are historical records, not canonical behavior specs.
- For current behavior, prefer `docs/spec/` and `docs/architecture/`.
