# PR #84 Fixes Changelog

**Date:** 2025-06-15\
**PR:** #84 (dspy-go agent integration)

## Summary

Addressed 23 tasks from PR review comments including 5 CI failures and 18 code
review suggestions.

## CI Fixes (Tasks 1-5)

**Root Cause:** GCC linking failures due to `CGO_ENABLED=1` in Makefile targets.

**Fix:** Switched all non-race targets to `CGO_ENABLED=0`:

- `Makefile`: Updated `build`, `test`, `test-short`, `cover`, `check-coverage`,
  `skills-build`, `skills-test` to use `GO_CMD` (CGO off)
- `.github/workflows/ci.yml`: Coverage step now uses `CGO_ENABLED=0`

Per AGENTS.md, the project uses CGO off by default with `modernc.org/sqlite`
(pure Go).

## Code Review Fixes (Tasks 6-23)

### Data Races & Concurrency (Tasks 7-10)

- **overseer.go:78**: Use thread-safe `o.runtime.List()` instead of direct map
  access
- **overseer.go:151**: Fixed `RootActorID` to always be `OverseerActorID` (tree
  root)
- **overseer.go:172**: Use `FindSessionByActorID` + `Get` for thread-safe
  session lookup
- **overseer.go:289**: Fixed busy-wait loop with proper polling sleep + context
  cancellation

### LLM Provider Switching (Task 11)

- **runtime.go**: Implemented dynamic provider switching (`gemini`, `openai`)
- Precedence: AgentConfig → RuntimeConfig → env var → default
- Added `defaultModelForProvider()` helper
- Default models: `gemini-2.5-flash`, `gpt-4.1-mini`

### Tool Improvements (Tasks 12-20)

- **code_tools.go:64**: Use `exec.CommandContext` for cancellation support
- **code_tools.go:83**: Remove `--json` flag to use text output matching parser
- **code_tools.go:71**: Fix ineffectual assignment to `searchPath`
- **mail_tools.go**: Add context cancellation checks to all three stubs
- **spawn_tools.go:101**: Handle both `[]any` and JSON string for
  `requested_subagents`
- **test_tools.go:115**: Use `filepath.Join` for cross-platform path
  construction
- **tools.go**: Remove duplicate import, integrate `PathValidator` for secure
  path resolution

### Code Quality (Task 21)

- **factory_test.go:337**: Replace recursive `contains` with `strings.Contains`
- **todo/main.go:337**: Add nil check before accessing `in.Plan.Goal`

### Documentation (Task 22)

- **agent_runtime_refactors.md**: Remove non-existent `gemini-3-pro-preview`
  model
- Fixed `gemini-2.5-flash-lite` to `gemini-2.0-flash-lite` per Google API docs

### Concurrency (Task 23)

- **overseer.go:250**: Add session locking in `buildHierarchyNode`
- Prevents data race when reading session fields during hierarchy traversal
- Copy fields to locals under lock, release before recursion to avoid deadlocks

### Security (Task 24)

- **run_dspy_test.sh:10**: Remove hardcoded API key (replaced with placeholder)

### Lint Fixes (errcheck)

- **dspy_agent.go:212,215,223**: Handle `fmt.Fprintln`, `fmt.Fprintf`, `w.Flush`
  returns
- **code_tools.go:157**: Handle `fmt.Sscanf` return
- **hierarchy_spawn_test.go:151,152,190,208,226**: Handle `rt.Kill` returns

### Lint Fixes (revive)

- **runtime.go**: Rename `RuntimeConfig` → `Config` to fix stuttering
- **tools.go**: Rename `ToolsConfig` → `Config` to fix stuttering
- **types.go**: Add `//revive:disable:var-naming` for intentional package name
- **types.go**: Add doc comments for all `Role*` and `Status*` constants

All references updated in CLI, tests, and docs.

## Verification

```bash
# Build passes with CGO off
CGO_ENABLED=0 go build ./...

# Tests pass (without API keys set)
env -u GROQ_API_KEY -u OPENAI_API_KEY -u FOXCTL_LLM_API_KEY \
  CGO_ENABLED=0 go test -short ./...
```
