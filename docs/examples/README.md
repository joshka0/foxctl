# Examples

Practical usage examples for workflows and command sequences.

These are illustrative references; command flags and behavior should be verified against current CLI help (`agentctl --help` and subcommand help).
