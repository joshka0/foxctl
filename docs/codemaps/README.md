# Codemap Artifacts

This directory contains generated codemap outputs and supporting notes.

## Notes

- Most `.codemap` files are generated artifacts.
- Treat these as analysis snapshots, not canonical docs.
- Regenerate when code drifts to keep traces accurate.
