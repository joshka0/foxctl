# Tailnet Validation Evidence — 2026-04-10

## Environment

- gateway host: local macOS machine on tailnet
- second device: `dev-machine.tailbf79e9.ts.net`
- gateway hostname: `test-validation`
- emitted tsnet FQDN: `test-validation.tailbf79e9.ts.net`

## Observations

### Gateway startup

- `tsnet node online`
- `dns_name=test-validation.tailbf79e9.ts.net.`
- `tailscale_ip=100.99.232.96`
- `Gateway HTTPS server listening on tsnet`
- `SSH server listening on tsnet (port 22)`

### VAL-GW-022 — Auto-TLS HTTPS

From `dev-machine.tailbf79e9.ts.net`:

```bash
curl -svf https://test-validation.tailbf79e9.ts.net/healthz
```

Observed:

- TLS handshake succeeded
- certificate verified successfully
- certificate subject/SAN matched `test-validation.tailbf79e9.ts.net`
- HTTP `200 OK`
- body:

```json
{
  "tsnet": "ok",
  "store": "ok",
  "tmux": "ok"
}
```

Verdict: `pass`

### VAL-GW-023 — MagicDNS hostname

From the gateway host:

- `nslookup test-validation.tailbf79e9.ts.net` resolved to `100.99.232.96`

From `dev-machine.tailbf79e9.ts.net`:

- FQDN HTTPS access succeeded:

```bash
curl -svf https://test-validation.tailbf79e9.ts.net/healthz
```

- short-name HTTPS probe resolved but failed certificate hostname validation:

```bash
curl -svf https://test-validation/healthz
```

Observed failure:

- `subjectAltName does not match hostname test-validation`
- `SSL: no alternative certificate subject name matches target hostname 'test-validation'`

Verdict: `pass` for MagicDNS/FQDN reachability, with a documented short-name TLS limitation.

### VAL-GW-024 — Tailnet-only access

Local host probe:

```bash
curl -svf http://localhost:8765/healthz
```

Observed:

- connection refused on `::1` and `127.0.0.1`

This confirms non-`--dev` mode is not serving localhost.

Outside-tailnet probe from the same host after disconnecting Tailscale:

```bash
curl -svf https://test-validation.tailbf79e9.ts.net/healthz
```

Observed:

- `Could not resolve host: test-validation.tailbf79e9.ts.net`
- local `tailscale status` could no longer reach the local daemon

Verdict: `pass`; from off-tailnet, the gateway FQDN is not reachable.

## Final status

- `VAL-GW-022`: pass
- `VAL-GW-023`: pass for MagicDNS/FQDN reachability, with a documented short-name TLS limitation
- `VAL-GW-024`: pass
