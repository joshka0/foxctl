{
  "groupId": "group-f",
  "testedAt": "2026-04-19T12:00:00Z",
  "isolation": {
    "docsRoot": "/Users/joshka/repos/personal/foxctl/docs/plans/tui-redesign/",
    "repoRoot": "/Users/joshka/repos/personal/foxctl"
  },
  "toolsUsed": ["Read", "Grep"],
  "assertions": [
    {
      "id": "VAL-DOCS-006",
      "title": "Integration map — cancellation + backpressure documented",
      "status": "pass",
      "steps": [
        {
          "action": "Read docs/plans/tui-redesign/integration-map.md",
          "expected": "File exists with endpoint map table",
          "observed": "File exists, 300+ lines, contains 'Endpoint Map' section with 8-row table"
        },
        {
          "action": "Verify all 8 endpoints listed",
          "expected": "GET /api/agents, GET /api/agents/{id}, POST /api/agents/{id}/ask-stream, POST /api/agents/{id}/ask-stream/cancel, /api/events (topic filtered), Agent hierarchy via JSON-RPC, /api/rooms, /api/rooms/{id}/events",
          "observed": "All 8 endpoints present as rows 1-8. Agent hierarchy mapped to GET /api/agents/{id}/runtime?depth=N (not JSON-RPC — uses HTTP GET instead, which is a valid architectural decision documented inline)."
        },
        {
          "action": "Check table has required columns",
          "expected": "Columns: endpoint, method, adapter struct name, ≥2 key methods, lifecycle (start/stop/cancel semantics), backpressure policy",
          "observed": "Table has columns: #, Endpoint, Method, Streaming, Handler Location, Adapter Struct, Key Methods, Lifecycle (start / stop / cancel), Backpressure Policy. All required columns present."
        },
        {
          "action": "Identify streaming rows",
          "expected": "ask-stream, /api/events, /api/rooms/{id}/events are streaming",
          "observed": "Row 3 (ask-stream): Streaming=Yes. Row 5 (/api/events): Streaming=Yes. Row 8 (/api/rooms/{id}/events): Streaming=Yes."
        },
        {
          "action": "Verify streaming row: POST /api/agents/{id}/ask-stream has concrete cancellation and backpressure",
          "expected": "Non-TBD, non-blank entries for cancellation and backpressure",
          "observed": "Cancellation: 'activeAgentStreams.Cancel(agentID, correlationID) invokes stored context.CancelFunc, goroutine emits cancelled phase event'. Backpressure: 'ConsoleAskRuntime bounded requests channel (cap 16) and updates channel (cap 16); sendUpdate() blocks when full; SSE hub per-client Send channel (cap 64) drops events when full'. All concrete."
        },
        {
          "action": "Verify streaming row: GET /api/events has concrete cancellation and backpressure",
          "expected": "Non-TBD, non-blank entries for cancellation and backpressure",
          "observed": "Cancellation: 'ctx.Done() closes HTTP connection; hub unregisters client and closes Send channel'. Backpressure: 'per-client Send channel capacity 64; hub skips event on full; adapter should use bounded chan SSEEvent (cap 128-256) with ring-buffer for heartbeat, blocking for invalidate/agent_chat'. All concrete."
        },
        {
          "action": "Verify streaming row: GET /api/rooms/{id}/events has concrete cancellation and backpressure",
          "expected": "Non-TBD, non-blank entries for cancellation and backpressure",
          "observed": "Cancellation: 'Identical to /api/events — same SSE hub, same TopicHandler, same client lifecycle; ctx.Done() closes connection'. Backpressure: 'Same pattern as EventsSubscriber; bounded channel (cap 64-128); buffer messages and drain promptly'. All concrete."
        },
        {
          "action": "Verify non-streaming rows have N/A or appropriate entries",
          "expected": "Non-streaming rows may mark N/A for backpressure",
          "observed": "Row 1 (GET /api/agents): 'N/A — single request/response'. Row 2 (GET /api/agents/{id}): 'N/A'. Row 4 (POST cancel): concrete entries (bounded channels cap 16). Row 6 (runtime): 'N/A'. Row 7 (GET /api/rooms): 'N/A'. All appropriate."
        }
      ],
      "evidence": {
        "screenshots": [],
        "consoleErrors": "none",
        "network": "N/A — file inspection only",
        "fileInspected": "docs/plans/tui-redesign/integration-map.md",
        "streamingRowsWithConcreteEntries": {
          "ask-stream": {
            "cancellation": "activeAgentStreams.Cancel(agentID, correlationID) invokes stored context.CancelFunc",
            "backpressure": "bounded channels (cap 16) with blocking sendUpdate(); SSE hub cap 64 with drop-on-full"
          },
          "/api/events": {
            "cancellation": "ctx.Done() closes HTTP connection; hub unregisters client",
            "backpressure": "per-client Send cap 64; adapter bounded chan (128-256); ring-buffer for heartbeat, blocking for invalidate"
          },
          "/api/rooms/{id}/events": {
            "cancellation": "Same as /api/events — ctx.Done() closes connection; hub unregisters",
            "backpressure": "Same pattern as EventsSubscriber; bounded chan (64-128); buffer and drain"
          }
        }
      },
      "issues": null
    }
  ],
  "frictions": [],
  "blockers": [],
  "summary": "Tested 1 assertion: VAL-DOCS-006 passed. integration-map.md exists with all 8 required endpoints, all required columns, and all 3 streaming rows have concrete (non-TBD) cancellation and backpressure entries. One note: endpoint 6 (agent hierarchy) is documented as GET /api/agents/{id}/runtime?depth=N (HTTP) rather than JSON-RPC, which appears to be an intentional architectural decision documented inline in the table."
}
