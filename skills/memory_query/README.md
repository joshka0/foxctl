# memory/query

Searches named memories by hybrid vector + BM25 lexical recall. Returns
ranked results with similarity scores and source attribution.

## Usage

```bash
foxctl skills run memory/query --query "user preferences" --limit 10
```
